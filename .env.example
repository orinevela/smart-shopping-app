DATABASE_URL="postgresql://user:password@host:5432/dbname"
JWT_SECRET="dev-secret-change-me"

# Only needed to call POST /api/admin/seed once after the first deploy
# (there's no shell access to a Vercel deployment's database).
SEED_SECRET="dev-secret-change-me"
